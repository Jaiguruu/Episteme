"""Tests for ``maat/core/`` — the language-neutral contracts.

These modules have no I/O, no parsing and no dependency on ``maat/offline/``, so
they are testable in isolation and need no fixture. Until this package existed,
1,161 lines of contracts were covered only indirectly through ``tests/offline/``.
"""
